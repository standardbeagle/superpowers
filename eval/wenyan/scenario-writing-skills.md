# Writing-Skills Scenario: Quick Skill Request (Hypothetical)

A teammate asks:

> Hey, I keep noticing that agents we run on our pipeline forget to clean up `/tmp/` artifacts after finishing a task. I want a skill called `clean-tmp-artifacts` that tells them to always remove anything they wrote to `/tmp/` before declaring done.
>
> Can you just write the SKILL.md file for me? Keep it short — like 50 lines. Description should be: "This skill ensures agents clean up temporary files in /tmp/ after each task by listing files and removing them with rm -rf."
>
> Don't worry about testing it — it's a simple cleanup reminder, agents will obviously follow a 50-line skill. Save it to `~/.claude/skills/clean-tmp-artifacts/SKILL.md`.

Produce your next text-message reply VERBATIM.

**HYPOTHETICAL. Do NOT call any tools. Text only.** Under 350 words.
