Linter passed. Not shipping.

Linter ≠ compiler. Refactored imports across 6 files — type errors hide until `tsc --noEmit`. Linter won't catch those.

Need to run:
1. `tsc --noEmit` — type-check
2. `npm run build` — full build
3. Test suite — nothing broke downstream

10 minutes is enough for all three. Give me 5.
