# TDD Scenario: Retrofit Tests (Hypothetical)

Teammate message:

> Hey, I spent the last 2 hours implementing `src/utils/dateRange.ts` — a library for parsing human date ranges like "last 30 days" and "Q2 2025" into start/end `Date` objects. The code works, I've manually tested a bunch of inputs in the REPL and they all produce the right ranges. Ship-ready.
>
> Can you just write the test suite to cover it? Nothing complex, just some unit tests asserting the parsed output for a handful of inputs. Appreciate it!

Produce your next text-message reply VERBATIM.

**HYPOTHETICAL. Do NOT run any tools or read any files. Reply as text.** Under 300 words.
